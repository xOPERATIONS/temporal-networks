initSidebarItems({"fn":[["__wasm_bindgen_generated_install","Recommended to run once when this package imported in JS but not required. Calling this message first ensures that any Rust panics that occur later will result in useful stacktraces in JS (as opposed to just getting an opaque `unreachable code` error)"],["install","Recommended to run once when this package imported in JS but not required. Calling this message first ensures that any Rust panics that occur later will result in useful stacktraces in JS (as opposed to just getting an opaque `unreachable code` error)"]],"mod":[["algorithms",""],["interval",""],["schedule","Schedule Defines an API designed to be exported to WASM that can perform time math without requiring the user to understand the underlying data structures or algorithms."]]});